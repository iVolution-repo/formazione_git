/*
   @ts-nocheck
   Node.js - Your first Hello World application
   ----------------------------------------------------------
   Once you have Node.js installed, it's very simple to call
   Node.js scripts. Most common installs put the Node.js
   executable onto the path environment variable so that
   Node.js can be invoked from most shells.

   Steps:
   1. Create a new directory for this training (we will use
      nodejstraining)
   2. Launch a shell (cmd.exe or Git BASH on Windows, Terminal
      on macOS)
   3. Use the cd command (change directory) to move to the new
      directory you created.
      For example, if you created nodejstraining on your D
      drive, you would use cd D:/nodejstraining
   4. Open your favourite text editor and create helloworld.js
      in your training directory.
   5. Edit helloworld.js so it has the following content:
*/

console.log("Hello world!");

/*
   6. Use the node command to invoke your new script.

      d:\>cd nodejstraining
      d:\nodejstraining>node helloworld
      Hello world!

   Notice how when we use the node command, there is no
   extension on the file name - it is optional.
*/
