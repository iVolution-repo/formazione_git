/*
   Primitive variables - Undefined
   ----------------------------------------------------------
   undefined means the referencing value does not have a type.
   For example, if you declare a variable with no initial
   value, then it will be undefined.

   You can also assign undefined to variable.
*/

// @ts-ignore
let x;
console.log(x);
//prints: undefined
